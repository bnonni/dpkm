version: '3'
services:
  drpm-registry:
    build: .
    container_name: drpm-registry-container
    ports:
      - "2092:2092"
    networks:
      - dpm_network
    environment:
      - VIRTUAL_HOST=registry.drpm.software.local

networks:
  dpm_network:
    driver: bridge
